/*
 * viene mostrato l'errore semantico inerente
 * il type mismatch in un'assegnazione multipla
 */


int n, c;

proc main() void:
    n, c := 10, 10.10; /*viene assegnato un float ad un intero*/
    write("qualcosa");
    ->
corp;